export type AdditionalFormData = {
  nome: string;
  preco: string;
};

export type AdditionalPayload = {
  nome: string;
  preco: number;
  produtoId: number;
};

export type AdditionalPreview = {
  id: number;
  nome: string;
  preco: number;
  produtoId: number;
};
